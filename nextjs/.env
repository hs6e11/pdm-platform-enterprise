# Backend FastAPI base URL
BACKEND_URL=http://localhost:8000

# Optional refresh endpoint (if your backend supports it)
BACKEND_REFRESH_PATH=/auth/refresh

# Optional sensor stream (SSE or websocket) endpoints for live charts
SENSOR_SSE_URL=
SENSOR_WS_URL=
